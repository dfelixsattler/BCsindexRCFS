% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/height_age_conversions.R
\name{si_age_to_ht}
\alias{si_age_to_ht}
\title{Calculate height given site index and age}
\usage{
si_age_to_ht(
  cu_index = NULL,
  age,
  age_type = 1,
  site_index,
  y2bh = NA_real_,
  pi = 0.5,
  species = NULL,
  curve = "default",
  fiz = NULL
)
}
\arguments{
\item{cu_index}{numeric or integer, explicit curve index}

\item{age}{numeric, age to convert}

\item{age_type}{integer, defines age type. Must be one of: `0`, the age is the
total age of the stand in years since planting; `1`, the age indicates the
number of years since the stand reached breast height.}

\item{site_index}{numeric, site index value}

\item{y2bh}{numeric, years to breast height (optional)}

\item{pi}{numeric, projection index (default: 0.5)}

\item{species}{integer/numeric species index or species code (e.g. "SW", "FDI")}

\item{curve}{curve selector when species is provided: "default", "first", numeric curve index, or curve name}

\item{fiz}{optional FIZ code used when remapping species codes}
}
\value{
numeric height
}
\description{
Given site index and age, computes tree height. Age can be given as total age or breast height age.
Site index must be based on breast height age 50. Where breast height age is less than 0,
a quadratic function is used. If `y2bh` is not provided it will be estimated via `si_y2bh`.
}
\examples{
si_age_to_ht(1, 50, 1, 30)
si_age_to_ht(age = 50, site_index = 30, species = "SW")
}
