---
title: "Legacy Interfaces Reference"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Legacy Interfaces Reference}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

## Overview

SIndexR provides modern, harmonized wrapper functions that offer cleaner APIs and better integration with the package ecosystem. Some legacy functions are retained for backward compatibility but are no longer the recommended approach for new code.

This document lists all legacy interfaces and their modern equivalents.

## Legacy Conversion Wrappers

These legacy functions perform core site index conversions but return results in a `list(output=..., error=...)` format and require numeric curve indices. Modern wrappers accept species codes, have cleaner return values, and integrate with external DLL backends.

### Height to Site Index

| Legacy Function | Modern Function | Key Improvements |
|-----------------|-----------------|------------------|
| `SIndexR_HtAgeToSI(curve, age, ageType, height, estType)` | `ht_age_to_si(cu_index = NULL, age, age_type = 1, height, si_est_type = 0, species = NULL, curve = "default", fiz = NULL)` | Accepts species codes, simpler return value, external DLL support |

**Example:**
```r
# Old way (legacy)
result <- SIndexR_HtAgeToSI(1, 50, 1, 30, 0)
si <- result$output

# New way (modern)
si <- ht_age_to_si(1, 50, 1, 30, 0)
# Or with species code:
si <- ht_age_to_si(age = 50, height = 30, species = "SW")
```

### Age to Height (Site Index)

| Legacy Function | Modern Function | Key Improvements |
|-----------------|-----------------|------------------|
| `SIndexR_AgeSIToHt()` *(removed)* | `si_age_to_ht(cu_index = NULL, age, age_type = 1, site_index, y2bh = NA_real_, pi = 0.5, species = NULL, curve = "default", fiz = NULL)` | Accepts species codes, simpler return value, external DLL support |

**Example:**
```r
# New way (modern)
height <- si_age_to_ht(1, 50, 1, 30, 2)
# Or with species code:
height <- si_age_to_ht(age = 50, site_index = 30, species = "SW")
```

### Height to Age (Site Index)

| Legacy Function | Modern Function | Key Improvements |
|-----------------|-----------------|------------------|
| `SIndexR_HtSIToAge(curve, height, ageType, siteIndex, y2bh)` | `si_ht_to_age(cu_index = NULL, site_height, age_type = 1, site_index, y2bh = NA_real_, species = NULL, curve = "default", fiz = NULL)` | Accepts species codes, simpler return value, external DLL support |

**Example:**
```r
# New way (modern)
age <- si_ht_to_age(1, 30, 1, 30, 2)
# Or with species code:
age <- si_ht_to_age(site_height = 30, site_index = 30, species = "SW")
```

### Site Class to Site Index

| Legacy Function | Modern Function | Key Improvements |
|-----------------|-----------------|------------------|
| `SIndexR_SCToSI(sp_index, sitecl, fiz)` | `SC2SI(species, site_class, fiz = NULL)` | Accepts species codes, simpler return value, cleaner validation |

**Example:**
```r
# Old way (legacy)
result <- SIndexR_SCToSI(11, "M", "H")
si <- result$output

# New way (modern)
si <- SC2SI(11, "M", "H")
# Or with species code:
si <- SC2SI("FDI", "M")
```

### Years to Breast Height (0.5-year steps)

| Legacy Function | Modern Function | Key Improvements |
|-----------------|-----------------|------------------|
| `SIndexR_Y2BH05(curve, siteIndex)` | `si_to_y2bh05(cu_index = NULL, site_index, species = NULL, curve = "default", fiz = NULL)` | Accepts species codes, simpler return value, external DLL support |

**Example:**
```r
# Old way (legacy)
result <- SIndexR_Y2BH05(1, 30)
y2bh <- result$output

# New way (modern)
y2bh <- si_to_y2bh05(1, 30)
# Or with species code:
y2bh <- si_to_y2bh05(site_index = 30, species = "SW")
```

## Return Value Changes

Legacy functions return `list(output = ..., error = ...)` while modern wrappers return the numeric result directly.

```r
# Legacy pattern
result <- SIndexR_HtAgeToSI(1, 50, 1, 30, 0)
if (result$error == 0) {
  si <- result$output
} else {
  cat("Error code:", result$error)
}

# Modern pattern
si <- ht_age_to_si(1, 50, 1, 30, 0)
if (is.numeric(si) && si > 0) {
  # Use si directly
}
```

## Deprecation Warnings

When you use a legacy function, SIndexR emits a warning on first use in your session:

```
Warning: SIndexR_HtAgeToSI() is a legacy compatibility interface; 
prefer ht_age_to_si() for new code.
```

This warning appears only once per session to avoid cluttering output.

## Migration Tips

1. **Replace direct function names**: Simply swap the old name for the new one.
2. **Use species codes**: Instead of remembering numeric curve indices, use species codes like `"SW"`, `"FDI"`, `"HWC"`.
3. **Simplify return handling**: Modern functions return numeric values directly, eliminating the need to extract from a list.
4. **Enable external DLL support**: Modern wrappers automatically use external SIndex DLLs if configured via `SIndexR_SetExternalDll()`.

## Modernization Matrix for Still-Active Interfaces

The functions below are not currently superseded conversion wrappers. They remain active and useful, but can be modernized additively for API consistency.

Recommended action categories:

- **Keep**: leave behavior and naming unchanged.
- **Add modern alias**: introduce a new wrapper name/shape while retaining the original.
- **Arguments-only modernization**: keep function purpose but align argument names and defaults.

| Existing Function | Role | Recommendation | Modernization Value | Notes |
|-------------------|------|----------------|---------------------|-------|
| `SIndexR_CurveName` | Curve metadata lookup | Keep | Low | Stable utility; clear single-purpose behavior |
| `SIndexR_CurveNotes` | Curve metadata lookup | Keep (hidden) | Low | Replaced by `curve_notes`; legacy kept for compatibility |
| `SIndexR_CurveSource` | Curve metadata lookup | Removed | — | Function not found; removed from package |
| `SIndexR_CurveToSpecies` | Curve-to-species mapping | Removed | — | Function not found; removed from package |
| `SIndexR_CurveUse` | Curve usage flag/metadata | Removed | — | Function not found; removed from package |
| `SIndexR_DefCurve` | Default curve for species | Removed | — | Function not found; removed from package |
| `SIndexR_DefCurveEst` | Default curve estimation mode | Renamed | Medium | Modern name: `default_curve_estab()` |
| `SIndexR_DefGICurve` | Default GI curve lookup | Renamed | Medium | Modern name: `default_gi_curve()` |
| `SIndexR_FirstSpecies` | Species iteration start | Add modern alias | Medium | Candidate alias: `SpeciesFirst()` for readability |
| `SIndexR_NextSpecies` | Species iteration step | Add modern alias | Medium | Candidate alias: `SpeciesNext()` and iterator-style docs |
| `SIndexR_SpecCode` | Species index to code | Add modern alias | High | Candidate alias: `species_code()` with vectorized behavior |
| `SIndexR_SpecName` | Species index to full name | Add modern alias | High | Candidate alias: `species_name()` with vectorized behavior |
| `SIndexR_SpecUse` | Species usage flag/metadata | Keep | Medium | Keep for now; modernize only if semantics need clarification |
| `SIndexR_VersionNumber` | Core SIndex version | Add modern alias | Medium | Candidate alias: `SIndexVersion()` for discoverability |

### Suggested Rollout Order

1. **High value aliases first**: `SIndexR_DefCurve`, `SIndexR_SpecCode`, `SIndexR_SpecName`.
2. **Navigation aliases next**: `SIndexR_FirstSpecies`, `SIndexR_NextSpecies`, `SIndexR_VersionNumber`.
3. **Keep specialized metadata functions unchanged** until concrete user friction appears.

### Implementation Guardrails

1. Add modern wrappers as pure delegates first (no behavioral change).
2. Keep legacy functions exported and documented as compatibility interfaces.
3. Add equivalence tests for old/new pairs before any deprecation warnings.
4. Use once-per-session warnings only after the new interfaces are proven stable.

## Backward Compatibility

All legacy functions remain available and functional. Code using legacy interfaces will continue to work without modification. However, new code should prefer the modern wrappers for better usability and future enhancements.
